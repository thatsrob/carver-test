{
	"name": "carver-llc",
	"private": true,
	"workspaces": ["carver"],
	"scripts": {
		"build": "npm run build -w carver",
		"dev": "npm run dev -w carver",
		"build:pages": "npm run build:pages -w carver"
	}
}
