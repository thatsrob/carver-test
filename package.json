{
	"name": "carver-llc",
	"private": true,
	"scripts": {
		"postinstall": "npm ci --prefix carver --include=dev",
		"build": "npm run build --prefix carver",
		"dev": "npm run dev --prefix carver",
		"build:pages": "npm run build:pages --prefix carver"
	}
}
